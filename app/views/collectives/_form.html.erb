<%= form_for(@collective) do |f| %>
  <% if @collective.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collective.errors.count, "error") %> prohibited this collective from being saved:</h2>

      <ul>
      <% @collective.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :collective_id %><br />
    <%= f.number_field :collective_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :collective_name %><br />
    <%= f.text_field :collective_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
